apiVersion: v1
kind: Service
metadata:
  name: external-postgres-service
spec:
  selector:{}
  ports:
    - name: postgres
      protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 0

